# Build notes

The feature branch is intentionally isolated from `main` until GitHub Actions reports a successful clean install and Next.js build.

A failed build must be resolved before merge or deployment.
